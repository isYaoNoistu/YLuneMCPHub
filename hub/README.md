# 月弦 — 控制台视觉标尺

这是**设计标尺**，不是运行中的产品。登录后的 React 控制台必须往本页的结构与令牌靠拢：

- 黑底 `#0a0a0b` + 单一月光青 `#5eead4`
- 侧栏品牌、青色左标线、底栏用户 + `LOGOUT`
- 顶栏面包屑 + 时钟 + `ONLINE`
- 统计卡 / 分组卡 / 端点复制 / 胶囊按钮

静态页里部分导航曾标 `SOON`，只表示当时标尺稿还没画完。**产品功能以 React 控制台为准。**

接视觉时改 `frontend/`，不要把本目录当成唯一前端。
